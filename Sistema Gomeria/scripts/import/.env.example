# Variables de entorno para scripts/import — NO commitear con valores reales.
# Copiar a `.env` en esta misma carpeta o exportar antes de correr runImport.ts.

SUPABASE_URL=https://aaplvlvewjeovitpyscg.supabase.co
SUPABASE_SERVICE_ROLE_KEY=service-role-jwt-aca

# Path al .gdb work copy (no tocar el original)
GDB_PATH=C:/temp/fbwork/DataBRPVta_work.gdb

# Tienda destino — si tu instalación tiene 1 sola tienda, usá su UUID acá.
# Los customers/receipts del legacy se importan con import_store_id = STORE_ID.
STORE_ID=stores-uuid-de-tienda-baliña

# Carpeta de outputs del parser (full-schema). El loader lee de acá.
SCHEMA_DIR=c:/Users/octav/OneDrive/Escritorio/sistema octabalina/Sistem-octabalina/gdb-analysis/full-schema
